interface:
  display_name: "Setup Workflow"
  short_description: "Configure workflow and Symphony adoption"
  default_prompt: "Use $setup-workflow to inspect this repository and draft its Agent Workflow and Symphony configuration."
policy:
  allow_implicit_invocation: false
