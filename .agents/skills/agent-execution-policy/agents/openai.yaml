interface:
  display_name: "Agent Execution Policy"
  short_description: "Load the shared delivery policy for assigned work"
  default_prompt: "Use $agent-execution-policy for this explicitly assigned workflow run."
policy:
  allow_implicit_invocation: true
